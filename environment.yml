name: thought-hbm
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - torch>=2.1
    - transformers>=4.40
    - accelerate
    - datasets
    - numpy
    - pandas
    - matplotlib
    - seaborn
    - scipy
    - tqdm
