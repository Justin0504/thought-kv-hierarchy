# thought-kv-hierarchy
Semantics-aware memory hierarchy for KV-cache in LLM reasoning. Offloads low-importance reasoning tokens from HBM to DDR instead of evicting them, preserving accuracy at reduced memory cost.
